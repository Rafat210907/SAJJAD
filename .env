# Front-end (Vite) will read this base URL for the reviews API.
# In production replace with your bot server domain (https://your-domain.com or similar).
VITE_REVIEWS_API_BASE=http://localhost:4000

# Port for the integrated reviews API + Discord bot (override if needed)
REVIEWS_PORT=4000

# (Optional) Move secrets like DISCORD_TOKEN out of config.json:
# DISCORD_TOKEN=your-token-here
# Then adjust code to read process.env.DISCORD_TOKEN instead of config.json.
